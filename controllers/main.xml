<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_pdf_controller" model="ir.actions.server">
        <field name="name">PDF Controller</field>
        <field name="model_id" ref="model_pdf_document"/>
        <field name="state">code</field>
        <field name="code">
            action = env['pdf.document'].process_via_controller()
        </field>
    </record>
</odoo>